# run this program by doing
#
# python /the/folder/to/here/
# without the filename
#
# it will locate the file __main__.py as its entry point
#
# also the folder in question is in sys.path
# and so we can import just 'run'

from run import main

main()
